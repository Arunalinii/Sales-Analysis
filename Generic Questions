------------ Generic Question----------------------
----------- how many unique cities does the data have-------------

select distinct city from salesdata.sales;

select count(distinct city) from salesdata.sales;

------------------- In which city is each branch?------

select distinct branch from salesdata.sales;

select distinct city, branch from salesdata.sales;
